name = "jeelani"
new_name = name

name+= "khader"
print(id(name))
print(id(new_name))

#immutable objects destroys the previous address and move to new address when other object is bounded to it ex : str , bool , etc